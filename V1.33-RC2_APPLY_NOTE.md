# V1.33.0-RC2 Asset Loading Stabilization

## 対象

- 適用元: `RSS Reader Modernization 1.33.0-RC1`
- 適用先: `RSS Reader Modernization 1.33.0-RC2`
- 正式版: 引き続き`V1.32.0`
- RC2は正式Releaseではありません。

## 変更内容

- Dashboardから動的に読み込むJavaScriptを、既存の宣言・依存順で1本ずつ開始するQueueへ変更。
- 動的Stylesheetは既存のCascade順を維持し、4本ずつ100ms間隔で開始。
- JavaScript／Stylesheetの読込失敗時だけ、600ms後に同じAssetを1回再試行。
- 再試行URLには`asset_retry=1`を付与し、失敗ResponseのCache影響を避ける。
- 2回失敗したAssetはConsoleへ記録し、無限再試行しない。
- RC1 Cacheから新しいLoaderへ確実に切り替えるため、Application／Asset RevisionをRC2へ更新。

## 変更していないもの

- Calendarの画面、日／週／月表示、予定の描画方法。
- 予定の登録、編集、削除、繰り返し、Occurrence処理。
- Calendar APIおよび`api_v1.php`。
- 登録・更新・削除Requestの自動再送。
- Database、Migration、Config、認証・認可・CSRF等のSecurity境界。

## 上書きファイル

- `app/version.php`
- `public/js/calendar.js`
- `public/js/camera-video-streaming.js`
- `public/js/rss-management.js`

後者2ファイルは動作変更ではなく、内部AssetのCache RevisionをRC2へ揃えるための更新です。

## 適用手順

1. 現在のRC1の`app/version.php`と`public/js/`、またはApplication全体をBackupする。
2. ZIPを本番Directory外へ展開する。
3. ZIP内の`app/`と`public/`を、同じ相対PathでRC1へ上書きする。
4. `config/local.php`、Database、`var/`、秘密鍵、Runtime Dataは変更しない。
5. RC2では新しいSQLを実行しない。Migration 025をRC1までに適用済みなら再実行不要。
6. Browserを完全Reloadし、Version表示が`RSS Reader Modernization 1.33.0-RC2`であることを確認する。

## 本番確認手順

1. DevToolsのNetworkでCacheを無効にし、Dashboardを完全Reloadする。
2. `calendar.js?v=1.33.0-rc2`がHTTP 200かつJavaScript MIMEで返ることを確認する。
3. 動的JavaScriptが宣言順に読み込まれ、Calendar、Mail、Camera、X等の利用中Widgetが欠けないことを確認する。
4. 動的CSSがHTTP 200かつCSS MIMEで返り、Calendarを含む画面デザインがRC1から変わっていないことを確認する。
5. Cache無効の完全Reloadを10回行い、503、MIME不一致、JavaScript Errorの有無を確認する。
6. 通常Cacheへ戻してReloadを10回行い、同じ項目を確認する。
7. 一時的なAsset失敗が発生した場合、同じURLの`asset_retry=1`が最大1回だけ発生し、HTTP 200で回復するか確認する。
8. `api_v1.php`等のPOSTがLoaderによって再送されていないことをNetworkで確認する。
9. Calendarの日／週／月切替、前後移動、今日、予定追加を確認する。
10. 既存予定の表示、予定編集、削除、繰り返し予定、Occurrence単位操作を確認する。
11. 複数日予定の連結、14:00–15:00等の時間軸配置がRC1と同じことを確認する。
12. RSS、Stock、Memo、Task、Mail、Camera、X等、実際に利用しているWidgetの初期化を確認する。
13. PCとSmartphoneで初期表示と操作を確認する。
14. Console、Network、取得可能なError logに新しい異常がないことを確認する。

## Rollback

RC1でBackupした同じ4ファイルを戻し、Browser Cacheを完全Reloadします。DB変更はないため、RC2専用のDB Rollbackはありません。

## 既知の制限

- `calendar.js`本体が取得できない場合は、その内部Loaderも開始できません。今回観測された子Assetの断続的503を主対象とします。
- Server全体が継続して503の場合はClient側再試行だけでは回復できません。
- JavaScriptを安全優先で順次開始するため、Cacheが空の初回表示はRC1より僅かに遅くなる可能性があります。
