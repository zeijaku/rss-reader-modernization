# RSS Reader Modernization V1.33-F

## 適用範囲

このZIPは、正式版 `v1.32.0` に対して `app/`、`public/`、`database/` をプロジェクトルートへ上書きする累積オーバーレイです。V1.33-B/C/D/Eの変更を含み、V1.33-FではCalendar月表示の複数日予定を週内で連結し、重複予定の表示段を固定します。

表示Versionは `RSS Reader Modernization 1.33.0-dev.5` です。

## 適用順

1. 本番ファイルとDBのバックアップを取得する。
2. ZIPを展開し、プロジェクトルートへ `app/`、`public/`、`database/` を上書きする。
3. `database/migrations/025_v1_33_calendar_event_exception.sql` を、V1.33-D以降で未適用の場合だけ実行する。既に適用済みの場合は再実行不要です。
4. PHP OPcacheを利用している場合は再検証または再起動を行う。
5. ブラウザをハードリロードし、古いCalendar JavaScript/CSSを破棄する。
6. ログイン後、下記の確認手順を上から実行する。

## V1.33-Fの変更

- 複数日予定を `single`、`start`、`middle`、`end` の日別セグメントへ分類。
- 同じ週のセグメントへ共通laneを割り当て、隣接する日で1本の予定に見えるよう表示。
- 土曜日でいったん区切り、翌日の日曜日から継続表示。週境界には継続記号を表示。
- 月初より前から続く予定、月末より後へ続く予定にも継続記号を表示。
- 重複する複数日予定には別laneを割り当て、途中で上段が空いても同じ予定が上下へ移動しないよう不可視placeholderを挿入。
- 単日予定、Task、取消済みOccurrence、Occurrence Overrideを同じ配置処理へ統合。
- 連結した各日セグメントに既存のOccurrence識別情報を保持し、どの部分を選んでもV1.33-Eの個別編集導線を維持。
- Smartphone幅では連結量と高さを縮小し、7列を既存カード幅へ収める。
- タイトル・メモのHTML表示方法は変更せず、既存のtext挿入と属性設定を維持。

## 変更ファイル

V1.33-F固有の主な変更ファイルは次のとおりです。

- `public/js/calendar-month-layout.js`（新規）
- `public/css/calendar-month-layout.css`（新規）
- `public/js/calendar-core.js`
- `public/js/calendar.js`
- `public/js/camera-video-streaming.js`（共通asset revision更新のみ）
- `public/js/rss-management.js`（共通asset revision更新のみ）
- `app/version.php`

ZIPには正式版v1.32.0からのV1.33-B〜F累積変更ファイルも含まれます。

## DB / Config / API

- V1.33-F固有のDB変更: なし。
- 累積ZIPにはV1.33-DのAdditive Migration `025_v1_33_calendar_event_exception.sql` を含む。
- Config変更: なし。
- 新規API action / 新規public PHP endpoint: なし。V1.33-Cの `calendar.range.list` データを再利用。

## Securityへの影響

- Authentication、Authorization / Owner Scope、CSRF、Session、Step-up Authentication、2FA、Recovery Code、Session Registry、Authentication Security Audit Logの処理変更なし。
- 配置moduleはDOM、通信、DBへ直接アクセスしない純粋なクライアント処理。
- Calendarタイトルは引き続きjQueryの `.text()` で生成し、`innerHTML`、`eval`、`document.write`を追加していない。
- 既存のOccurrence keyとrevisionを各セグメントへ渡し、V1.33-EのOwner Scope・楽観的競合制御を維持。

## こちらでの確認手順

1. Dashboard下部などのVersion表示が `RSS Reader Modernization 1.33.0-dev.5` になっていることを確認する。
2. 同じ週の3日間（例: 9/10〜9/12）にまたがる終日予定を作り、3個の独立ラベルではなく1本につながった予定に見えることを確認する。
3. 2日間にまたがる時刻付き予定を作り、開始側と終了側が連結し、開始・終了時刻の既存表示が破綻しないことを確認する。
4. 土曜から翌週の日曜以降へ続く予定を作り、土曜でいったん区切られ、日曜から同じタイトルの継続行として再開することを確認する。
5. 月末から翌月へ続く予定を作り、当月末に次月への継続記号が出ること、翌月表示へ移動すると前月からの継続記号が出ることを確認する。
6. 同じ期間へ2〜3件の複数日予定を重ね、各予定が別の段に並び、期間途中で上下の段が入れ替わらないことを確認する。
7. 複数日予定と同じ日に単日予定・Taskを追加し、いずれも消えず、クリックして既存の編集モーダルを開けることを確認する。
8. 繰り返し予定の1回だけを複数日に変更し、そのOccurrenceだけが連結表示され、連結したどの日から選択しても同じ個別Occurrenceを編集できることを確認する。
9. 繰り返しOccurrenceを取り消し、取消済み表示から復元でき、他のOccurrenceへ影響しないことを確認する。
10. Blue / Green / Red / Yellow / Purpleの各色で連結表示を確認し、開始・中間・終了の境界と文字を識別できることを確認する。
11. Default Theme、Bootstrap Solar、Bootstrap Slateで確認し、背景・文字・継続記号のContrastが不自然でないことを確認する。
12. Smartphone幅で手順2、4、6を確認し、7列がカード外へはみ出さず、連結・段固定・タップ操作が維持されることを確認する。
13. 月の前後移動を素早く行い、古い月の応答で表示が戻らないこと、移動後も連結表示されることを確認する。
14. タイトルに `<b>確認</b>` のような文字列を含む予定を作り、HTMLとして解釈されず文字として表示されることを確認する。

## 既知の制限

- 1個のDOMを複数セルへまたがらせず、日ごとのDOMをCSSで連結して見せる方式です。
- 週境界ではCalendarの行に合わせて分割し、次の週の先頭セグメントにタイトルを再表示します。
- 狭いWidgetや重複件数が多い月では、既存のカード内縦方向表示量が増えます。
- Drag & Drop、日表示、週表示、「これ以降」の繰り返し編集はV1.33-Fの対象外です。
- 実ブラウザでの最終的な見た目・Theme・Smartphone確認は、上記手順で本番相当環境から確認してください。
