# RSS Reader Modernization V1.33-E

## 適用範囲

このZIPは、正式版 v1.32.0 に対して `app/`、`public/`、`database/`、`tests/` を上書きする累積オーバーレイです。V1.33-B/C/Dの変更を含み、V1.33-EではCalendarのOccurrence単位操作UIを追加しています。

## 適用順

1. 本番ファイルのバックアップを取得する。
2. ZIPの内容をプロジェクトルートへ上書きする。
3. `database/migrations/025_v1_33_calendar_event_exception.sql` を、未適用の場合だけ実行する。既に適用済みの場合は再実行不要です（冪等チェック付き）。
4. PHPのOPcacheまたはブラウザキャッシュを必要に応じて更新する。
5. ログイン後、下記の確認手順を実行する。

## V1.33-Eの変更

- 繰り返し予定の変更モーダルに「この予定のみ」「シリーズ全体」を追加。
- 個別変更したOccurrenceの取消と、個別変更・取消の復元を追加。
- 個別操作にはOccurrenceの元日付と楽観的Concurrency tokenを使用。
- 取消済みOccurrenceを月表示へ控えめに表示し、復元導線を追加。
- 月表示と直近予定を個別操作後に別々に再取得。
- シリーズ全体の更新処理は既存のRecurrence APIへ委譲。
- 既存の3色を含む5色管理、CSRF、Owner Scope、XSS Escape、入力検証を維持。

## DB / Config

- V1.33-E固有のDB変更なし。
- V1.33-Dで追加した `025_v1_33_calendar_event_exception.sql` が累積ZIPに含まれます。
- Config変更なし。

## こちらでの確認手順

1. Calendar Widgetを開き、繰り返し予定の通常Occurrenceを選択する。
2. 「この予定のみ」が初期選択され、対象日・タイトル・色・時刻を確認する。
3. タイトルまたは日付を変更して保存し、その回だけ変わることを確認する。
4. 同じシリーズの別の回が変わっていないことを確認する。
5. 変更した回を再度開き、「この回の個別変更を元に戻す」で復元する。
6. 別の回を開き、「この予定のみ取り消す」を実行し、月表示で取消済みとして表示されることを確認する。
7. 取消済み回を開き、「この回の個別変更を元に戻す」でシリーズ内容へ戻ることを確認する。
8. 別の繰り返し予定を開き、「シリーズ全体」を明示的に選択して変更し、個別変更済みの回が保持されることを確認する。
9. 月表示を移動した後、直近予定に戻り、両方の表示が最新状態になることを確認する。
10. スマートフォン幅で同じ操作を行い、範囲選択・ボタン・モーダルが横スクロールなしで操作できることを確認する。
11. 未ログイン状態、別ユーザーの予定、CSRF不正、古い画面を二重送信した場合に、既存の拒否・競合表示が維持されることを確認する。

## 既知の制限

- 「これ以降」の分割編集はV1.33-Eの対象外です。
- 取消済みOccurrenceは月表示の元のOccurrence日へ表示します。直近予定は表示対象が通常の予定のみです。
- MariaDB実サーバーを使ったmigration実行は、実行環境にMariaDBクライアント／サーバーがある場合に行ってください。
