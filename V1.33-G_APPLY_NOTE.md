# RSS Reader Modernization V1.33-G

## 適用範囲

このZIPは、正式版 `v1.32.0` に対して `app/`、`public/`、`database/` をプロジェクトルートへ上書きする累積オーバーレイです。V1.33-B/C/D/E/Fの変更を含み、V1.33-GではCalendarへ日表示・週表示・月表示の切替、日表示の24時間軸、狭いWidgetとSmartphone向けの週一覧を追加しています。

表示Versionは `RSS Reader Modernization 1.33.0-dev.6` です。

## 適用順

1. 本番ファイルとDBのバックアップを取得する。
2. ZIPを展開し、プロジェクトルートへ `app/`、`public/`、`database/` を上書きする。
3. `database/migrations/025_v1_33_calendar_event_exception.sql` を、V1.33-D以降で未適用の場合だけ実行する。既に適用済みの場合は再実行不要です。
4. PHP OPcacheを利用している場合は再検証または再起動を行う。
5. ブラウザをハードリロードし、古いCalendar JavaScript/CSSを破棄する。
6. ログイン後、下記の「こちらでの確認手順」を上から実行する。

## V1.33-Gの変更

- Calendarへ「日・週・月」の表示切替を追加。初期表示は従来と同じ月表示。
- Widgetごとに選択日と表示モードをクライアント上で保持。DBやWidget設定へは保存しない。
- 前後ボタンを、日表示では前日/翌日、週表示では前週/翌週、月表示では前月/翌月として動作させる。
- 「今日」で、現在の表示モードを維持したまま今日を含む期間へ戻る。
- 月の日付から予定追加Modalを開く既存操作を維持し、その日を選択日として保持する。
- 日表示へ「終日・複数日」と「時間指定」を分けた表示を追加。
- 日表示の時間指定予定は00:00〜24:00の時間軸へ配置し、重複予定を横に分割。
- 終了時刻未設定は「開始のみ」、開始と終了が同じ場合は「同時刻」として区別し、架空の終了時刻は表示しない。
- 時刻を持つ複数日予定は無理に単日の時間軸へ展開せず、「終日・複数日」欄へ表示。
- 週表示は日曜始まりの7日列とし、Fのstart/middle/end連結と段固定を再利用。
- 実際のCalendarカード幅を`ResizeObserver`で監視し、720px未満では週表示を日付別の縦一覧へ変更。
- `ResizeObserver`がない環境でも、575.98px以下ではCSSによりSmartphone用の日付別一覧へ変更。
- 月跨ぎ・年跨ぎ・2000/2100境界では、既存APIの対応範囲内へ期間を制限。
- Default、Bootstrap Solar、Bootstrap Slateの既存Theme変数を利用。
- Calendarタイトル・メモは従来どおりtext/属性として設定し、HTMLとして挿入しない。

## V1.33-G固有の変更ファイル

- `app/view/dashboard_widgets.php`
- `app/version.php`
- `public/stock.php`
- `public/js/calendar-core.js`
- `public/js/calendar-views.js`（新規）
- `public/js/calendar.js`
- `public/js/calendar-polish-r3.js`
- `public/js/rss-management.js`（共通Asset Revision更新）
- `public/js/camera-video-streaming.js`（共通Asset Revision更新）
- `public/css/calendar-views.css`（新規）
- `public/css/calendar-colors.css`

ZIPには正式版v1.32.0からのV1.33-B〜G累積変更ファイルも含まれます。

## DB / Config / API

- V1.33-G固有のDB変更：なし。
- 累積ZIPにはV1.33-DのAdditive Migration `025_v1_33_calendar_event_exception.sql`を含む。
- Config変更：なし。
- 新規API action：なし。
- 新規public PHP endpoint：なし。
- 日/週/月はいずれもV1.33-Cの認証・CSRF保護された`calendar.range.list`を使用。
- サーバー側の42日、予定元500件、Occurrence 2000件、Task 500件の上限は変更していない。

## Securityへの影響

- Authentication、Authorization / Owner Scope、CSRF、Session、Step-up Authentication、2FA、Recovery Code、Session Registry、Authentication Security Audit Logの処理変更なし。
- 期間のOwnerは引き続き認証済みSessionから取得し、クライアントから指定しない。
- 表示切替によってDB書込みは発生しない。
- 日時・重複計算moduleはDOM、通信、DBへ直接アクセスしない純粋なクライアント処理。
- 時間軸のinline CSS変数は、検証済み時刻から計算した数値だけを設定。
- タイトル・メモに`innerHTML`、`eval`、`document.write`を追加していない。
- Occurrence key、original start、revisionを日/週/月の予定buttonへ保持し、D/EのOwner Scopeと楽観的競合制御を維持。

## テスト結果

| 分類 | 結果 | 内容 |
| --- | --- | --- |
| G専用計算・DOM・静的契約 | PASS | 62項目 / FAIL 0 |
| Calendar関連Node/Python Regression | PASS | 441項目 / FAIL 0 |
| Account Security静的Regression | PASS | 169項目 / FAIL 0 |
| JavaScript Syntax | PASS | Calendar関連13ファイル |
| Python Syntax | PASS | G静的契約テスト |
| Git diff check | PASS | whitespace errorなし |
| PHP Syntax / PHP Focused Test | SKIP | 作業環境にPHP CLIなし |
| PHP 8.1 / PHP 8.4 | SKIP | Final GateのV1.33-Hで実行 |
| MariaDB実Migration / Backend Test | SKIP | 作業環境にMariaDB/PHPなし。G固有DB変更なし |
| 実ブラウザ / Smartphone実機 | SKIP | 作業環境にブラウザランタイムなし |
| tests/run-current.sh | SKIP | 全体RegressionはV1.33-Hへ集約 |
| tests/run-current-features.sh | SKIP | PHP CLIが必要。構成へGテストは追加済み |
| Package / Clean-room / Release Gate | SKIP | 正式な全体GateはV1.33-H |

最初のG Focused Testで、日表示ラベルがAPI応答の`range_start / range_end`を認識せず`----`になる不一致を1件検出しました。修正後にG専用・C〜F関連・Security静的Regressionを再実行し、FAIL 0を確認しています。

## こちらでの確認手順

1. Dashboard下部などのVersion表示が`RSS Reader Modernization 1.33.0-dev.6`になっていることを確認する。
2. Calendarが最初は従来どおり月表示で開き、Fで追加した複数日予定の連結表示が維持されていることを確認する。
3. 「日」「週」「月」を順に切り替え、押したモードが選択状態になり、期間ラベルと表示内容が切り替わることを確認する。
4. 月表示で任意の日付を押して予定追加Modalを一度開き、閉じてから「日」へ切り替え、その日の日表示になることを確認する。
5. 日表示で前後ボタンを押し、前日/翌日へ1日ずつ移動すること、「今日」で本日に戻ることを確認する。
6. 週表示で前後ボタンを押し、前週/翌週へ7日単位で移動すること、「今日」で本日を含む週に戻ることを確認する。
7. 月表示で前後ボタンを押し、従来どおり前月/翌月へ移動することを確認する。
8. 日表示で、終日予定、Task、複数日予定が「終日・複数日」欄に表示され、クリックして既存の編集Modalを開けることを確認する。
9. 同じ日の09:00〜11:00と09:30〜10:00など重なる予定を作り、日表示の時間軸で横に分かれ、両方をクリックできることを確認する。
10. 終了時刻を設定しない予定を作り、「開始のみ」と表示されることを確認する。開始・終了を同じ時刻にした予定は「同時刻」と表示されることを確認する。
11. 00:00開始、深夜帯、23:00開始の予定を作り、時間軸をスクロールして欠落せず表示されることを確認する。
12. 週表示で3日間の複数日予定を確認し、広いCalendarカードでは日曜始まりの7日列上でstart/middle/endがつながって見えることを確認する。
13. 月末〜翌月および12月末〜翌年1月の週を表示し、期間ラベル、7日の日付、予定が破綻しないことを確認する。
14. Calendar Widgetの横幅を1へ変更するか、ブラウザ上でカード幅を狭くし、週表示が7本の細い列ではなく日付別の縦一覧へ切り替わることを確認する。
15. Smartphone幅で日/週/月を切り替え、Toolbarがカード外へはみ出さず、週表示が日付別一覧になり、予定をタップできることを確認する。
16. Calendar Widgetを2つ配置し、それぞれ別の表示モード・日付へ移動しても、片方の操作で他方の表示が切り替わらないことを確認する。
17. 繰り返し予定の1回だけを変更・取消し・復元し、日/週/月の各表示へ反映され、他のOccurrenceへ影響しないことを確認する。
18. Blue / Green / Red / Yellow / Purpleの予定を日/週/月で確認し、特に狭い週一覧でも色の識別が残ることを確認する。
19. Default、Bootstrap Solar、Bootstrap Slateで、日付、時間罫線、予定文字、休日、選択中モードのContrastを確認する。
20. タイトルに`<b>確認</b>`、メモに`<script>alert(1)</script>`のような文字列を入れ、HTMLやScriptとして実行されず文字として扱われることを確認する。
21. 日/週/月を素早く切り替え、古い通信応答で以前の表示へ戻らないこと、二重に予定が描画されないことを確認する。
22. Calendar以外のRSS、Stock、Task、Account Security画面を簡単に確認し、通常操作と認証状態にRegressionがないことを確認する。

## 既知の制限

- 表示モードと選択日はページ内のWidgetごとに保持しますが、再読込後は月表示・今日へ戻ります。
- 週表示は予定を日単位で確認するWidget向け表示で、Google Calendarのような7列の詳細時間軸ではありません。
- 日表示の終了未設定と同時刻予定は、視認可能な最小30分相当の高さを使いますが、表示文言やデータ上の終了時刻は補完しません。
- 時刻を持つ複数日予定は、日表示では「終日・複数日」欄へ表示します。
- 週表示の広い/狭い切替境界は720pxです。
- Drag & Drop、時間のResize、Timezone拡張、「これ以降」の繰り返し編集はV1.33-Gの対象外です。
- 実ブラウザでの最終的な見た目、Theme、Smartphone、Touch操作は上記手順で本番相当環境から確認してください。
